---
# file: ssh/tests/tasks
#
# Test tasks to verify role execution.
#

- include: configuration.yml
  tags: configuration

- name: test if global known hosts are setup correctly
  command: ssh-keygen {{ if ssh_known_hosts_hash then '-H' endif }} -f {{ ssh_known_hosts_path }} -F github.com
